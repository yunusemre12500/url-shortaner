apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - behavior: create
    env: .env
    options:
      disableNameSuffixHash: true
    name: api-server-secrets
resources:
  - deployment.yaml
  - service.yaml
namePrefix: url-shortaner-
